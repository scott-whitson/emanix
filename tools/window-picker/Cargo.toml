[package]
name = "window-picker"
version = "0.1.0"
edition = "2021"
description = "Hyprland per-window ace-jump window picker"
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
