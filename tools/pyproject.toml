[project]
name = "cli-tools"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "trafilatura>=2.0.0",
    "youtube-transcript-api>=1.2.4",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pillow>=11.0",
]
