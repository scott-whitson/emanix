[project]
name = "news"
version = "0.1.0"
description = "Daily tech news briefing from public RSS feeds"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
]

[tool.uv]
dev-dependencies = []