[project]
name = "calendar-sync"
version = "0.1.0"
description = "Two-way Google Calendar sync for Dates.org"
requires-python = ">=3.12"
dependencies = [
  "google-api-python-client>=2.140",
  "google-auth>=2.33",
  "google-auth-oauthlib>=1.2",
  "python-dateutil>=2.9",
]

[tool.uv]
