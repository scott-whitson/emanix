# Copy to ~/.config/photo-import/config.toml and edit.

ssh_target = "datacore"
archive_root = "/srv/data/photo-archive/Pictures"
immich_url = "http://datacore:2283"

# Optional overrides (defaults shown)
# staging_root = "~/downloads/camera"
# ledger_path = "~/.local/share/photo-import/imports.sqlite"
# log_path = "~/.local/share/photo-import/photo-import.log"
# retention_days = 30
