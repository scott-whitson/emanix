;;; scott-pi.el --- removed -*- lexical-binding: t; -*-
;; Pi-in-Emacs was removed because the vterm-based integration proved brittle.
;; Reintroduce a replacement launcher only after choosing a different approach.

(provide 'scott-pi)
;;; scott-pi.el ends here
