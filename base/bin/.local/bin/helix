#!/usr/bin/env bash
set -euo pipefail

if command -v hx &>/dev/null; then
	exec hx "$@"
fi

if command -v helix &>/dev/null; then
	exec helix "$@"
fi

echo "helix: neither hx nor helix found on PATH" >&2
exit 127
