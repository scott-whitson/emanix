.claude/
ventoy/dotfiles/
ventoy/.pat

# Python build artifacts
**/__pycache__/
**/*.pyc
**/*.pyo
**/.pytest_cache/

# Python virtualenvs
tools/.venv/
tools/photo-tools/.venv/

# Rust build output

# Large binary assets (not version-controlled; see docs for download instructions)
tools/vosk-models/*/

# Fresh-install read-only PAT (USB only; never commit)
ventoy/github-token
result

# Emacs byte-compiled artifacts
**/*.elc

# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/
